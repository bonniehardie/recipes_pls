import { makeStyles } from '@material-ui/core/styles';

const RecipeMenuStyles = makeStyles((theme) => ({
    container: {
        display: 'flex',
        flexDirection: 'row'
    }
}));

export default RecipeMenuStyles;
